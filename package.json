{
  "name": "docker-node-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "docker:build-image": "docker build -t docker-node-starter .",
    "docker:run-container": "docker run -d -p 3000:3000 docker-node-starter",
    "docker:compose": "docker-compose -f docker-compose.yml up -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
